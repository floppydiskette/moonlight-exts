{
  "apiLevel": 2,
  "id": "moonlight-css",
  "version": "1.5.0",
  "meta": {
    "name": "CSS",
    "tagline": "custom discord css for maximum hotdog theme",
    "authors": [
      "husky"
    ],
    "source": "https://github.com/floppydiskette/moonlight-exts",
    "tags": [
      "appearance"
    ]
  },
  "settings": {
    "cssPath": {
      "displayName": "CSS Source",
      "description": "File / Folder / URL to load CSS from",
      "type": "string",
      "default": ""
    },
    "fileSelector": {
      "displayName": "File Selection Regex",
      "description": "(Optional) Regex to match specific CSS files if using a folder",
      "type": "string",
      "default": ""
    },
    "recurseDirectory": {
      "displayName": "Recursively Search Folder",
      "description": "Search for CSS files in subdirectories if using a folder",
      "type": "boolean",
      "default": false
    }
  }
}
